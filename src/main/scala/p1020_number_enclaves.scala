package com.peacefulotter.leetcode

import scala.annotation.tailrec

object p1020_number_enclaves {
	// TODO: unsolved
	def numEnclaves(grid: Array[Array[Int]]): Int = {
		def rec(x: Int, y: Int, cache: Map[(Int, Int), Boolean], counter: Int): Int =
			???
		
		rec(0, 0, Map(), 0)
	}
}